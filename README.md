## Инструкция по сборке проекта и запуску базы данных:

1) Установка зависимостей: 

```bash
make install
```

2) Установка всех необходимых пакетов: 

```bash
make package-install
```

3) Запуск игры: 

```bash
make database
```

4) Сборка проекта: 

```bash
make build
```

5) Публикация:

```bash
make publish
```

6) Проверка кода в соответствии с ruff:

```bash
make lint
```

## Новые возможности через декораторы:

### 1. Обработка ошибок (`@handle_db_errors`)
Все функции, которые работают с таблицами или данными, теперь автоматически обрабатывают ошибки:

    `KeyError` — обращение к несуществующей таблице

    `ValueError` — ошибки валидации типов

    `FileNotFoundError` — ошибки чтения/записи файлов

### 2. Подтверждение опасных действий (`@confirm_action(action_name)`):
Функции удаления таблиц и записей требуют подтверждения пользователя:

```bash
Вы уверены, что хотите выполнить "действие"? [y/n]:
```

### 3. Логирование времени выполнения (`@log_time`):
Медленные операции, например `insert` и `select`, выводят время выполнения:

```bash
Функция "имя_функции" выполнилась за X.XXXXXX секунд
```

### 4. Кэширование результатов запросов (`create_cacher`):
Результаты одинаковых `select` запросов теперь кэшируются, чтобы ускорить повторное выполнение.

## Команды для взаимодействия с БД:

1) **create_table \<имя_таблицы\> \<столбец1:тип\> \<столбец2:тип\> ...** Cоздать таблицу
2) **drop_table \<имя_таблицы\>** Удалить таблицу
3) **list_tables** Показать список всех таблиц
4) **insert into \<имя_таблицы\> values (\<значение1\>, \<значение2\>, ...)**	Создать запись
5) **select from \<имя_таблицы\> where \<столбец\> = \<значение\>** Прочитать записи по условию
6) **select from \<имя_таблицы\>** Прочитать все записи
7) **update \<имя_таблицы\> set \<столбец1\> = \<новое_значение1\> where \<столбец_условия\> = \<значение_условия\>** Обновить запись
8) **delete from \<имя_таблицы\> where \<столбец\> = \<значение\>** Удалить запись
9) **info \<имя_таблицы\>** Вывести информацию о таблице
10) **exit** Выйти из программы
11) **help** Справочная информация

## 🎥 Демонстрация asciinema

[![Demo](https://asciinema.org/a/757922.svg)](https://asciinema.org/a/757922)